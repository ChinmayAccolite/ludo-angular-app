import { YellowTokenDirective } from './yellow-token.directive';

describe('YellowTokenDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowTokenDirective();
    expect(directive).toBeTruthy();
  });
});

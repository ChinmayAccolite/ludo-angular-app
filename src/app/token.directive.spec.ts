import { TokenDirective } from './token.directive';

describe('TokenDirective', () => {
  it('should create an instance', () => {
    const directive = new TokenDirective();
    expect(directive).toBeTruthy();
  });
});

import { GreenTokenDirective } from './green-token.directive';

describe('GreenTokenDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenTokenDirective();
    expect(directive).toBeTruthy();
  });
});

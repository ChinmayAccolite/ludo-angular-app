import { BlueTokenDirective } from './blue-token.directive';

describe('BlueTokenDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueTokenDirective();
    expect(directive).toBeTruthy();
  });
});
